import { contextOperationHandler } from "@/lib/context/contextOperationHandler";
/** Start ingestion, read progress, or build a brief using the shared Context Engine. */
export const POST = contextOperationHandler;
